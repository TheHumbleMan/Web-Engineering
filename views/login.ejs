<!doctype html>
<html lang="de">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Login</title>
    <link rel="icon" type="image/png" href="/favicon/study_icon.png">
    <link rel="stylesheet" href="/assets/style.css">
    <script defer src="/assets/theme.js"></script>
    <script defer src="/assets/password-toggle.js"></script>
</head>
<body style="align-items:center">

<header>
    <nav>
        <a href="/auth/register">Register</a>
        <a id="active-link" href="/auth/login">Login</a>
        <a href="/about">About</a>

        <label class="theme-toggle">
            <input type="checkbox" id="toggle">
            <span class="slider round">
                <div class="background"></div>
                <div class="star"></div>
                <div class="star"></div>
            </span>
        </label>

    </nav>
</header>

<% if (error) { %>
    <div id="error-message">
        <% if (error === 'invalid') { %>
            Ungültiger Benutzername oder Passwort.
        <% } else if (error === 'required') { %>
            Benutzername und Passwort sind erforderlich.
        <% } else if (error === 'session') { %>
            Ihre Sitzung ist abgelaufen. Bitte melden Sie sich erneut an.
        <% } else if (error === 'access') { %>
            Sie haben keine Berechtigung für diese Seite.
        <% } else if (error === 'locked') { %>
            Konto temporär gesperrt aufgrund zu vieler fehlgeschlagener Anmeldeversuche.
        <% } else if (error === 'inactive') { %>
            Das Konto ist deaktiviert.
        <% } else if (error === 'ratelimit') { %>
            Zu viele Login-Versuche. Bitte warten Sie eine Minute und versuchen Sie es erneut.
        <% } else { %>
            Ein Fehler ist aufgetreten.
        <% } %>
    </div>
<% } %>

<% if (success) { %>
    <div id="success-message">
        <% if (success === 'logout') { %>
            Sie wurden erfolgreich abgemeldet.
        <% } else if (success === 'created') { %>
            Registrierung erfolgreich. Bitte melden Sie sich an.
        <% } else if (success === 'login') { %>
            Anmeldung erfolgreich.
        <% } else { %>
            Erfolgreich.
        <% } %>
    </div>
<% } %>

<div id="login-form">
    <h1>Login</h1>
    <form method="POST" action="/auth/login">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <label for="username">Benutzername</label>
        <input id="username" name="username" type="text" required autofocus>

        <label for="password">Passwort</label>
        <div class="password-field">
            <input id="password" name="password" type="password" required>
            <button type="button" class="password-toggle" data-password-toggle="password" aria-label="Passwort anzeigen">Anzeigen</button>
        </div>

        <button type="submit">Anmelden</button>
    </form>
</div>

</body>
</html>
