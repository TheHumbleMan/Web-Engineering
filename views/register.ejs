<!doctype html>
<html lang="de">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Register</title>
    <link rel="icon" type="image/png" href="/favicon/study_icon.png">
    <link rel="stylesheet" href="/assets/style.css">
    <script defer src="/assets/theme.js"></script>
    <script defer src="/assets/password-toggle.js"></script>
</head>
<body>

<header>
    <nav>
        <a id="active-link" href="/auth/register">Register</a>
        <a href="/auth/login">Login</a>
        <a href="/about">About</a>

        <label class="theme-toggle">
            <input type="checkbox" id="toggle">
            <span class="slider round">
                <div class="background"></div>
                <div class="star"></div>
                <div class="star"></div>
            </span>
        </label>

    </nav>
</header>

<% if (error) { %>
    <div id="error-message">
        <% if (error === 'invalid') { %>
            Ungültiger Benutzername oder Passwort.
        <% } else if (error === 'required') { %>
            Alle Pflichtfelder sind erforderlich.
        <% } else if (error === 'mismatch') { %>
            Die Passwörter stimmen nicht überein.
        <% } else if (error === 'password') { %>
            Passwort zu schwach. Mindestens 8 Zeichen sowie Großbuchstaben, Kleinbuchstaben, Zahl und Sonderzeichen sind erforderlich.
        <% } else if (error === 'exists') { %>
            Dieser Benutzername ist bereits vergeben.
        <% } else if (error === 'session') { %>
            Ihre Sitzung ist abgelaufen. Bitte melden Sie sich erneut an.
        <% } else if (error === 'access') { %>
            Sie haben keine Berechtigung für diese Seite.
        <% } else if (error === 'locked') { %>
            Konto temporär gesperrt aufgrund zu vieler fehlgeschlagener Anmeldeversuche.
        <% } else if (error === 'inactive') { %>
            Das Konto ist deaktiviert.
        <% } else { %>
            Ein Fehler ist aufgetreten.
        <% } %>
    </div>
<% } %>

<% if (success) { %>
    <div id="success-message">
        <% if (success === 'logout') { %>
            Sie wurden erfolgreich abgemeldet.
        <% } else if (success === 'created') { %>
            Registrierung erfolgreich. Bitte melden Sie sich an.
        <% } else { %>
            Erfolgreich.
        <% } %>
    </div>
<% } %>

<div id="login-form">
    <h1>Register</h1>
    <form method="POST" action="/auth/register">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <label for="prename">Vorname *</label>
        <input id="prename" name="prename" type="text" required autofocus>

        <label for="lastname">Nachname *</label>
        <input id="lastname" name="lastname" type="text" required>

        <label for="username">Benutzername *</label>
        <input id="username" name="username" type="text" required>

        <label for="passwordone">Passwort *</label>
        <div class="password-field">
            <input id="passwordone" name="passwordone" type="password" minlength="8" pattern="(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[\W_]).{8,}" title="Mindestens 8 Zeichen mit Großbuchstabe, Kleinbuchstabe, Zahl und Sonderzeichen" required>
            <button type="button" class="password-toggle" data-password-toggle="passwordone" aria-label="Passwort anzeigen">Anzeigen</button>
        </div>

        <label for="passwordtwo">Passwort bestätigen *</label>
        <div class="password-field">
            <input id="passwordtwo" name="passwordtwo" type="password" minlength="8" pattern="(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[\W_]).{8,}" title="Mindestens 8 Zeichen mit Großbuchstabe, Kleinbuchstabe, Zahl und Sonderzeichen" required>
            <button type="button" class="password-toggle" data-password-toggle="passwordtwo" aria-label="Passwort anzeigen">Anzeigen</button>
        </div>

        <p>Passwortregeln: mindestens 8 Zeichen, 1 Großbuchstabe, 1 Kleinbuchstabe, 1 Zahl und 1 Sonderzeichen.</p>

        <button type="submit">Registrieren</button>
    </form>
</div>
<footer>
    <p>© 2026 Lernhilfe - Studienprojekt der DHBW Ravensburg</p>
    <a href="/impressum">Impressum</a>
    <a href="/datenschutz">Datenschutzerklärung</a>
</footer>
</body>
</html>
