<!doctype html>
<html lang="de">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Register</title>
    <!--<link id="theme-style" rel="stylesheet" href="/assets/dark.css">-->
    <!--<script defer src="/assets/theme.js"></script>-->
    <link rel="stylesheet" href="/assets/style.css">
</head>
<body>

<nav id="navbar">
    <div id="nav-inner">
        <ul id="nav-center">
            <li><a id="active-link" href="/auth/register">Register</a></li>
            <li><a href="/auth/login">Login</a></li>
            <li><a href="/about">About</a></li>
        </ul>

        <div id="dark-mode-switch">
            <label id="toggle-switch">
                <input id="toggle" type="checkbox">
                <span id="slider">
                    <div id="background"></div>
                    <div id="star-1"></div>
                    <div id="star-2"></div>
                </span>
            </label>
        </div>
    </div>
</nav>

<% if (error) { %>
    <div id="error-message">
        <% if (error === 'invalid') { %>
            Ungültiger Benutzername oder Passwort.
        <% } else if (error === 'required') { %>
            Alle Pflichtfelder sind erforderlich.
        <% } else if (error === 'mismatch') { %>
            Die Passwörter stimmen nicht überein.
        <% } else if (error === 'exists') { %>
            Dieser Benutzername ist bereits vergeben.
        <% } else if (error === 'session') { %>
            Ihre Sitzung ist abgelaufen. Bitte melden Sie sich erneut an.
        <% } else if (error === 'access') { %>
            Sie haben keine Berechtigung für diese Seite.
        <% } else if (error === 'locked') { %>
            Konto temporär gesperrt aufgrund zu vieler fehlgeschlagener Anmeldeversuche.
        <% } else if (error === 'inactive') { %>
            Das Konto ist deaktiviert.
        <% } else { %>
            Ein Fehler ist aufgetreten.
        <% } %>
    </div>
<% } %>

<% if (success) { %>
    <div id="success-message">
        <% if (success === 'logout') { %>
            Sie wurden erfolgreich abgemeldet.
        <% } else if (success === 'created') { %>
            Registrierung erfolgreich. Bitte melden Sie sich an.
        <% } else { %>
            Erfolgreich.
        <% } %>
    </div>
<% } %>

<div id="login-form">
    <h1>Register</h1>
    <form method="POST" action="/auth/register">
        <label for="prename">Vorname *</label>
        <input id="prename" name="prename" type="text" required autofocus>

        <label for="lastname">Nachname *</label>
        <input id="lastname" name="lastname" type="text" required>

        <label for="username">Benutzername *</label>
        <input id="username" name="username" type="text" required>

        <label for="passwordone">Passwort *</label>
        <input id="passwordone" name="passwordone" type="password" required>

        <label for="passwordtwo">Passwort bestätigen *</label>
        <input id="passwordtwo" name="passwordtwo" type="password" required>

        <button type="submit">Registrieren</button>
    </form>
</div>

</body>
</html>
